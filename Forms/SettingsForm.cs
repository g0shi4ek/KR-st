using System.Windows.Forms;

namespace KR.Forms
{
    /// <summary>
    /// Variant 11: COM-port parameters are fixed by default.
    /// This form is kept as a stub (not shown at startup) to avoid
    /// breaking any remaining references in the project.
    /// </summary>
    public partial class SettingsForm : Form
    {
        public SettingsForm()
        {
            InitializeComponent();
        }
    }
}
